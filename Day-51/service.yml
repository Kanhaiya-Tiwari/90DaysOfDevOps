apiVersion: v1
kind: Service

metadata:
  name: action-service
  namespace: action-app

spec:
  type: ClusterIP   # default, but explicitly define
  selector:
    app: action

  ports:
    - name: http
      protocol: TCP
      port: 80        # service port
      targetPort: 80  # container port (FIXED)
